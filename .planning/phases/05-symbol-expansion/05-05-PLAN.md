---
phase: 05-symbol-expansion
plan: 05
type: execute
depends_on: ["05-01", "05-03", "05-04"]
files_modified:
  - config/experiments/symbols-matrix.yaml
  - config/experiments/symbols-btc-eth-only.yaml
  - .planning/phases/05-symbol-expansion/SYMBOL-BACKTEST-COMPARISON.md
  - .planning/phases/05-symbol-expansion/symbols-backtest-results.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
domain: node-trading-bot
---

<objective>
Re-run baseline-style backtest comparing 2-symbol vs 5-symbol universe with mock sentiment and Phase 4 MTF preset.

Purpose: Quantify trade-count and win-rate impact of expansion before Phase 6.
Output: `SYMBOL-BACKTEST-COMPARISON.md` + matrix results.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-entry-baseline/baseline-backtest.json
@.planning/phases/04-mtf-entry-alignment/mtf-matrix-results.json
@.planning/phases/02-backtest-experiments/EXPERIMENT-PROTOCOL.md
@config/experiments/symbols-expanded.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comparison matrix</name>
  <files>config/experiments/symbols-btc-eth-only.yaml, config/experiments/symbols-matrix.yaml</files>
  <action>
`symbols-btc-eth-only.yaml` — `mtf-recommended` settings but only BTCUSDT + ETHUSDT (apples-to-apples with Phase 4 winner config).

`symbols-matrix.yaml`:
```yaml
from: "2024-10-01"
to: "2024-12-31"
mockSentiment: true
experimentsDir: ./data/reports/experiments/symbols-phase5
runs:
  - id: symbols-btc-eth
    config: config/experiments/symbols-btc-eth-only.yaml
  - id: symbols-expanded
    config: config/experiments/symbols-expanded.yaml
```

Run:
```bash
npm run backtest-matrix -- --matrix config/experiments/symbols-matrix.yaml
```
  </action>
  <verify>test -f data/reports/experiments/symbols-phase5/experiments-index.json</verify>
  <done>Two-run comparison complete</done>
</task>

<task type="auto">
  <name>Task 2: Analysis and project update</name>
  <files>.planning/phases/05-symbol-expansion/SYMBOL-BACKTEST-COMPARISON.md, .planning/phases/05-symbol-expansion/symbols-backtest-results.json, .planning/PROJECT.md</files>
  <action>
`SYMBOL-BACKTEST-COMPARISON.md`:

- Compare btc-eth vs expanded: totalTrades, winRate, pnl, maxDrawdown
- **Caveat:** mock sentiment scales with symbol count — expect more trades with 5 symbols; compare winRate and per-symbol trade rate
- Reference Phase 1 (25 trades, 2 sym) and Phase 4 mtf-tighter-fib (22 trades, 2 sym)
- Recommendation: proceed to Phase 6 with 5-symbol default

Copy index to `symbols-backtest-results.json`.

Update `PROJECT.md` Active: mark symbol expansion complete; Key Decision row for 5-symbol universe.

Update `STATE.md` — Phase 5 plans complete / execution done per actual state (plan 05 only sets ready to execute; execution updates complete).

Update `ROADMAP.md` checkboxes when executing 05-05 only — for planning leave unchecked.

Add `README.md` in phase folder with commands.
  </action>
  <verify>test -f .planning/phases/05-symbol-expansion/SYMBOL-BACKTEST-COMPARISON.md</verify>
  <done>Comparison doc + results JSON exist</done>
</task>

</tasks>

<success_criteria>
- Expanded universe backtest runs without missing kline errors
</success_criteria>

<output>
Create `05-05-SUMMARY.md` after execution.
</output>
