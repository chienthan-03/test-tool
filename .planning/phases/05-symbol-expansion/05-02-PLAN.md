---
phase: 05-symbol-expansion
plan: 02
type: execute
depends_on: ["05-01"]
files_modified:
  - tests/unit/symbol-mapper.test.ts
  - tests/fixtures/rss/sol-alt-headline.xml
  - tests/fixtures/rss/bnb-xrp-headline.xml
  - .planning/phases/05-symbol-expansion/SYMBOL-MAPPING.md
domain: node-trading-bot
---

<objective>
Verify and extend RSS → symbol mapping for SOL, BNB, XRP on the expanded whitelist.

Purpose: `DEFAULT_ALIASES` already includes solana/sol, binance/bnb, ripple/xrp — need tests + fixtures proving whitelist gating works.
Output: Unit tests + fixture XML + `SYMBOL-MAPPING.md`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/news/symbol-mapper.ts
@tests/unit/symbol-mapper.test.ts
@tests/integration/rss-pipeline.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for new symbols</name>
  <files>tests/unit/symbol-mapper.test.ts</files>
  <action>
Add tests with whitelist `['BTCUSDT','ETHUSDT','SOLUSDT','BNBUSDT','XRPUSDT']`:

- `Solana rally lifts SOL` → `['SOLUSDT']`
- `BNB and XRP surge` → `['BNBUSDT','XRPUSDT']` (order by text position)
- `Dogecoin only` → `[]` (still filtered)

Run `npm test -- tests/unit/symbol-mapper.test.ts`.
  </action>
  <verify>npm test -- tests/unit/symbol-mapper.test.ts</verify>
  <done>All symbol-mapper tests pass</done>
</task>

<task type="auto">
  <name>Task 2: RSS fixtures and mapping doc</name>
  <files>tests/fixtures/rss/sol-alt-headline.xml, tests/fixtures/rss/bnb-xrp-headline.xml, .planning/phases/05-symbol-expansion/SYMBOL-MAPPING.md</files>
  <action>
Create minimal RSS fixtures (1 item each) with headlines tagging SOL, BNB, XRP.

Optional: extend `seed-signals-from-fixtures.ts` glob — fixtures auto-included.

`SYMBOL-MAPPING.md` table: alias → ticker → USDT symbol; note custom aliases via `symbolOverrides` not used yet.

Document adding new aliases in `DEFAULT_ALIASES` if needed (e.g. `binance coin` — only if tests fail).
  </action>
  <verify>test -f .planning/phases/05-symbol-expansion/SYMBOL-MAPPING.md</verify>
  <done>Fixtures + doc exist</done>
</task>

</tasks>

<success_criteria>
- Mapper extracts new symbols when whitelisted
</success_criteria>

<output>
Create `05-02-SUMMARY.md` after execution.
</output>
