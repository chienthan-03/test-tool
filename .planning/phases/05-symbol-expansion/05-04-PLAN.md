---
phase: 05-symbol-expansion
plan: 04
type: execute
depends_on: ["05-01", "05-03"]
files_modified:
  - tests/integration/market-symbols-smoke.test.ts
  - .planning/phases/05-symbol-expansion/MARKET-SMOKE.md
domain: node-trading-bot
---

<objective>
Smoke-test Binance Futures REST/WS paths for new symbols in testnet-safe or read-only fashion.

Purpose: Catch invalid symbol errors before sim/live.
Output: Integration smoke test + `MARKET-SMOKE.md`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/market/kline-cache.ts
@src/execution/backtest-replayer.ts
@config/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST exchange info smoke test</name>
  <files>tests/integration/market-symbols-smoke.test.ts</files>
  <action>
Create `tests/integration/market-symbols-smoke.test.ts`:

- Load `symbols-expanded.yaml` or default with 5 symbols
- For each symbol: fetch exchange info or latest kline via public Futures REST (`config.binance.baseUrl`) — use existing download/fetch helpers if available
- Assert HTTP OK and symbol status TRADING (or skip test if network blocked with clear message)

Keep test fast; no WS long-running unless已有 pattern in repo.

Run: `npm test -- tests/integration/market-symbols-smoke.test.ts`
  </action>
  <verify>npm test -- tests/integration/market-symbols-smoke.test.ts</verify>
  <done>Test passes or documents skip reason in SUMMARY</done>
</task>

<task type="auto">
  <name>Task 2: Sim stack note</name>
  <files>.planning/phases/05-symbol-expansion/MARKET-SMOKE.md</files>
  <action>
`MARKET-SMOKE.md`:

- REST results per symbol
- How to run `npm run dev -- validate --config config/experiments/symbols-expanded.yaml` (dry-poll optional)
- Testnet note: same symbol names on USDⓈ-M testnet if keys present
- Not in scope: full WS soak test
  </action>
  <verify>test -f .planning/phases/05-symbol-expansion/MARKET-SMOKE.md</verify>
  <done>Smoke doc complete</done>
</task>

</tasks>

<success_criteria>
- New symbols confirmed on Binance Futures API
</success_criteria>

<output>
Create `05-04-SUMMARY.md` after execution.
</output>
