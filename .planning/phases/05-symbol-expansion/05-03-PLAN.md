---
phase: 05-symbol-expansion
plan: 03
type: execute
depends_on: ["05-01"]
files_modified:
  - scripts/prefetch-klines.ts
  - package.json
  - .planning/phases/05-symbol-expansion/KLINE-CACHE.md
domain: node-trading-bot
---

<objective>
Ensure kline cache files exist for all five symbols on context + entry timeframes for the standard backtest window.

Purpose: Backtest matrix uses `skipDownload: true` — new symbols need cached `data/klines/{SYMBOL}_{interval}.json`.
Output: `prefetch-klines` script + `KLINE-CACHE.md` inventory.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/market/kline-cache.ts
@src/execution/backtest-replayer.ts
@config/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prefetch script</name>
  <files>scripts/prefetch-klines.ts, package.json</files>
  <action>
Create `scripts/prefetch-klines.ts`:

- Args: `--config`, `--from`, `--to` (default 2024-10-01 / 2024-12-31)
- Read `config.symbols`, `timeframes.context`, `timeframes.entry`
- For each symbol × interval: call existing `downloadKlines` from `kline-cache.ts` with warmup (match backtest-replayer warmup logic or 90 days before `from`)
- Print file path + candle count per cache file

npm script: `"prefetch-klines": "tsx scripts/prefetch-klines.ts"`

Run once for expanded universe:
```bash
npm run prefetch-klines -- --from 2024-10-01 --to 2024-12-31
```
  </action>
  <verify>test -f data/klines/SOLUSDT_4h.json && test -f data/klines/XRPUSDT_1d.json</verify>
  <done>Cache files for SOL, BNB, XRP on 1d and 4h exist</done>
</task>

<task type="auto">
  <name>Task 2: Cache inventory doc</name>
  <files>.planning/phases/05-symbol-expansion/KLINE-CACHE.md</files>
  <action>
`KLINE-CACHE.md`:

- Table: symbol × interval × file path × approximate bar count / date range
- Note gitignore on `data/klines/` — local-only, reproducible via prefetch command
- 1h files (for Phase 4 TF experiments) optional, not required for Phase 5 baseline
  </action>
  <verify>test -f .planning/phases/05-symbol-expansion/KLINE-CACHE.md</verify>
  <done>Inventory documents all required caches</done>
</task>

</tasks>

<success_criteria>
- Backtest can run skipDownload for 5 symbols without error
</success_criteria>

<output>
Create `05-03-SUMMARY.md` after execution.
</output>
