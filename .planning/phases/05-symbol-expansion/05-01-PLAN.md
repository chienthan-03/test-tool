---
phase: 05-symbol-expansion
plan: 01
type: execute
depends_on: []
files_modified:
  - config/default.yaml
  - .planning/phases/05-symbol-expansion/SYMBOL-CONFIG.md
  - config/experiments/symbols-expanded.yaml
domain: node-trading-bot
---

<objective>
Add SOLUSDT, BNBUSDT, XRPUSDT to the production symbol whitelist and document config impact.

Purpose: Unblocks mapper, backtest, sim, and market subscriptions for top alts.
Output: Updated `config/default.yaml` + `SYMBOL-CONFIG.md` + experiment preset `symbols-expanded.yaml`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@config/default.yaml
@src/config/schema.ts
@.planning/PROJECT.md
@.planning/phases/04-mtf-entry-alignment/mtf-recommended.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand symbols in config</name>
  <files>config/default.yaml, config/experiments/symbols-expanded.yaml</files>
  <action>
Update `config/default.yaml` `symbols` list:

```yaml
symbols:
  - BTCUSDT
  - ETHUSDT
  - SOLUSDT
  - BNBUSDT
  - XRPUSDT
```

Create `config/experiments/symbols-expanded.yaml` — copy `mtf-recommended.yaml` (Phase 4 winner) with the same 5-symbol list and comment header `# experiment: symbols-expanded`.

Verify Zod `futuresSymbol` regex already accepts these tickers — **no schema change** unless validation fails.

Leave `symbolOverrides: {}` unless per-symbol risk tuning is documented as future work.
  </action>
  <verify>npm run lint && node -e "import('./dist/config/loader.js').then(m=>m.loadConfig('config/default.yaml')).catch(()=>process.exit(1))" 2>/dev/null || npx tsx -e "import { loadConfig } from './src/config/loader.js'; loadConfig('config/default.yaml'); console.log('ok')"</verify>
  <done>Config loads with 5 symbols</done>
</task>

<task type="auto">
  <name>Task 2: Document symbol config</name>
  <files>.planning/phases/05-symbol-expansion/SYMBOL-CONFIG.md</files>
  <action>
`SYMBOL-CONFIG.md`:

- Before/after symbol list
- Downstream consumers: `SymbolMapper`, `SignalMerger`, `BacktestReplayer`, `BinanceMarket` WS kline streams, `RiskEngine` per-symbol overrides
- Experiment presets to bulk-update (list `config/experiments/*.yaml` pattern — optional script note, not required in 05-01)
- Phase 6: no merge beyond default if already updated here
  </action>
  <verify>test -f .planning/phases/05-symbol-expansion/SYMBOL-CONFIG.md</verify>
  <done>Doc lists all touchpoints</done>
</task>

</tasks>

<success_criteria>
- Five symbols validated in default config
</success_criteria>

<output>
Create `05-01-SUMMARY.md` after execution.
</output>
