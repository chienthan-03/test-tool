---
phase: 01-entry-baseline
plan: 03
type: execute
depends_on: ["01-02"]
files_modified:
  - .planning/phases/01-entry-baseline/baseline-backtest.json
  - .planning/phases/01-entry-baseline/baseline-backtest-notes.md
domain: node-trading-bot
---

<objective>
Run baseline backtest(s) on BTC/ETH with default config and persist results for comparison in Phase 2.

Purpose: Establish quantitative starting point before filter experiments.
Output: `baseline-backtest.json` + short `baseline-backtest-notes.md`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-entry-baseline/01-METRICS-SCHEMA.md
@src/cli/commands/backtest.ts
@config/default.yaml
@data/klines/BTCUSDT_1d.json
@tests/integration/backtest-smoke.test.ts

**Tech:** `npm run dev -- backtest --from --to` prints JSON metrics.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determine valid backtest date range</name>
  <files>.planning/phases/01-entry-baseline/baseline-backtest-notes.md</files>
  <action>
Inspect `data/klines/` files for BTCUSDT and ETHUSDT (intervals available). Read `src/cli/backtest-dates.ts` for range limits.

In `baseline-backtest-notes.md` record:
- Chosen `--from` and `--to` ISO dates that fit fixture coverage
- Symbols tested (from config: BTCUSDT, ETHUSDT)
- Whether run uses `--mock-sentiment` (run TWO variants: with and without — document why each matters per 01-ENTRY-PATH.md)

If klines insufficient for full range, use shortest range that `npm run dev -- backtest` accepts and document limitation.
  </action>
  <verify>baseline-backtest-notes.md contains from/to dates and mock-sentiment decision</verify>
  <done>Date range validated against fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Execute backtest and save baseline JSON</name>
  <files>.planning/phases/01-entry-baseline/baseline-backtest.json</files>
  <action>
Run from repo root (adjust dates from Task 1):

```bash
npm run dev -- backtest --from <ISO> --to <ISO> --config config/default.yaml
npm run dev -- backtest --from <ISO> --to <ISO> --config config/default.yaml --mock-sentiment
```

Capture stdout JSON for each run. Write `baseline-backtest.json`:

```json
{
  "runs": [
    { "label": "default_sentiment", "from": "...", "to": "...", "metrics": { ... } },
    { "label": "mock_sentiment", "from": "...", "to": "...", "metrics": { ... } }
  ],
  "config_path": "config/default.yaml",
  "recorded_at": "<ISO datetime>"
}
```

If command fails, document error in notes and fix only blocking issues (do not change strategy logic — baseline must reflect current code).

Append to notes: interpretation (trade count, winRate) and caveats for Phase 2 comparison.
  </action>
  <verify>test -f .planning/phases/01-entry-baseline/baseline-backtest.json && npm run lint</verify>
  <done>At least one successful backtest run saved in JSON; notes explain caveats</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` passes
- [ ] `baseline-backtest.json` matches schema in 01-METRICS-SCHEMA.md
</verification>

<success_criteria>
- Reproducible baseline command documented in notes
- Metrics snapshot exists for Phase 2 experiment framework
</success_criteria>

<output>
Create `.planning/phases/01-entry-baseline/01-03-SUMMARY.md` after execution.
</output>
