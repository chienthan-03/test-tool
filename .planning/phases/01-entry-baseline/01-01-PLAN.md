---
phase: 01-entry-baseline
plan: 01
type: execute
depends_on: []
files_modified:
  - .planning/phases/01-entry-baseline/01-ENTRY-PATH.md
domain: node-trading-bot
---

<objective>
Document the complete entry decision path from RSS item to order placement with file paths and event names.

Purpose: Phase 2+ cannot improve win rate without a shared map of every gate and branch.
Output: `01-ENTRY-PATH.md` — sequence diagram (mermaid) + gate checklist.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@src/app/bootstrap.ts
@src/sentiment/news-pipeline.ts
@src/strategy/strategy-engine.ts
@src/risk/risk-engine.ts
@src/execution/adapter-factory.ts
@config/default.yaml

**Constraining decisions:** Research-first; manual trade review is primary metric.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace live path (start mode)</name>
  <files>.planning/phases/01-entry-baseline/01-ENTRY-PATH.md</files>
  <action>
Create `01-ENTRY-PATH.md` section "Live pipeline (`start`)". For each step list: trigger, module file, function/event, config keys that affect pass/fail.

Must include this chain with backtick paths:
1. `RssPoller` / `RssPollerManager` → `NewsRepository`
2. `SymbolMapper` whitelist (`config/default.yaml` `symbols`)
3. `NewsPipeline` → `RuleScorer` → optional `LlmGateway` (`sentiment.rules.thresholdLLM`, `minStrength`)
4. `SignalMerger` / signal storage
5. `StrategyEngine` + `MtfEngine` (timeframes `timeframes.context` / `timeframes.entry`)
6. `RiskEngine` → `OrderPlan` on bus event `risk:orderPlan`
7. `bootstrap.ts` `handleOrderPlan` → `ExecutionAdapter.placeEntry`

Add mermaid sequenceDiagram for live path. Note every early-exit (news dropped, symbol not mapped, strength too low, no intent).
  </action>
  <verify>File exists; grep -c "src/" 01-ENTRY-PATH.md returns ≥10 path references</verify>
  <done>Live path documented with ≥8 gates and file paths</done>
</task>

<task type="auto">
  <name>Task 2: Trace backtest path and diff vs live</name>
  <files>.planning/phases/01-entry-baseline/01-ENTRY-PATH.md</files>
  <action>
Append section "Backtest pipeline (`backtest`)" from `src/cli/commands/backtest.ts` and `src/execution/backtest-replayer.ts`. Document `mockSentiment` flag behavior.

Add table "Live vs backtest differences" (RSS, WS klines, LLM calls, adapter type, DB writes). List unknown parity risks for Phase 9.

Append "Entry gate checklist" — numbered list an operator can use when reviewing a single trade (what must be true for entry to fire).
  </action>
  <verify>01-ENTRY-PATH.md contains headings "Backtest pipeline" and "Entry gate checklist"</verify>
  <done>Both pipelines documented; checklist has ≥6 gates</done>
</task>

</tasks>

<verification>
- [ ] `01-ENTRY-PATH.md` is self-contained for a new reader
- [ ] No invented files — all paths exist in repo
</verification>

<success_criteria>
- Entry path doc complete for live + backtest
- Gate checklist ready for manual review workflow (Phase 8)
</success_criteria>

<output>
Create `.planning/phases/01-entry-baseline/01-01-SUMMARY.md` per GSD template after execution.
</output>
