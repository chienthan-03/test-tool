---
phase: 01-entry-baseline
plan: 02
type: execute
depends_on: ["01-01"]
files_modified:
  - .planning/phases/01-entry-baseline/01-METRICS-SCHEMA.md
  - .planning/phases/01-entry-baseline/review-template.csv
domain: node-trading-bot
---

<objective>
Define what to measure for "win rate per entry" and a CSV template for manual trade review.

Purpose: Aligns team on metrics before running backtest (plan 03) and export (plan 04).
Output: `01-METRICS-SCHEMA.md` + `review-template.csv` header row with example row.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-entry-baseline/01-ENTRY-PATH.md
@src/storage/repositories/trade-repo.ts
@src/cli/commands/backtest.ts
@src/execution/backtest-replayer.ts

**Issues being addressed:** Entry pain point unknown — schema makes review structured.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define baseline metrics</name>
  <files>.planning/phases/01-entry-baseline/01-METRICS-SCHEMA.md</files>
  <action>
Create `01-METRICS-SCHEMA.md` with:

**Automated (from backtest JSON output):** totalTrades, wins, losses, winRate, totalPnlUsdt, maxDrawdownPct — map to fields in `backtest.ts` console output.

**Per-trade (from `trades` table):** id, mode, symbol, side, entry_price, exit_price, pnl_usdt, stop_loss, take_profit, news_id, news_signal_id, opened_at, closed_at, status.

**Manual review columns (human fills):** setup_quality (1-5), news_quality (1-5), mtf_aligned (y/n), would_take_again (y/n), failure_category (enum: false_sentiment, bad_timing, sl_too_tight, sl_too_wide, noise, other), notes.

Define baseline snapshot format JSON:
```json
{ "config_path", "from", "to", "symbols", "mock_sentiment", "metrics": { ... } }
```

State primary success metric for project: manual `would_take_again` rate on sample of ≥20 closed trades (not only automated winRate).
  </action>
  <verify>test -f .planning/phases/01-entry-baseline/01-METRICS-SCHEMA.md</verify>
  <done>Schema documents automated + manual fields; primary metric stated</done>
</task>

<task type="auto">
  <name>Task 2: Create review CSV template</name>
  <files>.planning/phases/01-entry-baseline/review-template.csv</files>
  <action>
Create `review-template.csv` with header row matching manual review columns plus trade DB columns from Task 1.

Include ONE commented example row as second line starting with # describing a hypothetical losing trade (ASCII only, no real PII).

Do not add application code in this plan — export tooling is plan 04.
  </action>
  <verify>head -2 .planning/phases/01-entry-baseline/review-template.csv shows header + comment example</verify>
  <done>CSV template ready for export script to populate</done>
</task>

</tasks>

<verification>
- [ ] Metrics align with existing `trades` schema (read migrate.ts if needed)
- [ ] Manual columns support win-rate improvement goal from PROJECT.md
</verification>

<success_criteria>
- Reviewers know exactly which fields to fill
- Backtest output mapping documented for plan 03
</success_criteria>

<output>
Create `.planning/phases/01-entry-baseline/01-02-SUMMARY.md` after execution.
</output>
