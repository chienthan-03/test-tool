---
phase: 01-entry-baseline
plan: 04
type: execute
depends_on: ["01-02"]
files_modified:
  - scripts/export-trades-review.ts
  - package.json
domain: node-trading-bot
---

<objective>
Add a small CLI script to export closed trades to CSV matching the manual review template.

Purpose: Enables manual win-rate review without raw SQL.
Output: `scripts/export-trades-review.ts` invokable via `npm run dev --` or dedicated npm script.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-entry-baseline/review-template.csv
@.planning/phases/01-entry-baseline/01-METRICS-SCHEMA.md
@src/storage/repositories/trade-repo.ts
@src/storage/db.ts
@src/config/loader.ts

**Patterns:** ESM, tsx dev runner, kebab-case scripts in `scripts/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement export-trades-review script</name>
  <files>scripts/export-trades-review.ts, package.json</files>
  <action>
Create `scripts/export-trades-review.ts` that:
- Loads config via `loadConfigWithEnv` (default `config/default.yaml`)
- Opens SQLite at `config.storage.sqlitePath`
- Queries closed trades (`status = 'closed'`) ordered by `closed_at` DESC
- Writes CSV to path from `--out` flag (default: `.planning/phases/01-entry-baseline/trades-export.csv`)
- Columns: all DB trade fields from 01-METRICS-SCHEMA plus empty manual columns (setup_quality, news_quality, mtf_aligned, would_take_again, failure_category, notes) for human fill-in
- Optional `--limit N` (default 50)
- Prints row count to stderr

Add npm script `"export-trades-review": "tsx scripts/export-trades-review.ts"` if not duplicating dev pattern.

Use parameterized SQL only; no string concat for user input. Escape CSV fields properly.
Do not modify trade schema in this plan.
  </action>
  <verify>npm run lint && npm run export-trades-review -- --limit 5 --out /tmp/trades-test.csv 2>/dev/null || npm run dev -- export-trades-review -- --limit 5</verify>
  <done>Script runs without error; CSV has header matching review-template</done>
</task>

<task type="auto">
  <name>Task 2: Document usage in phase notes stub</name>
  <files>.planning/phases/01-entry-baseline/EXPORT-USAGE.md</files>
  <action>
Create `EXPORT-USAGE.md` with copy-paste commands:
- Export after sim/backtest populates DB
- How to open CSV and fill manual columns
- Link to `01-ENTRY-PATH.md` gate checklist when reviewing each row

If DB has zero closed trades, document that user should run backtest/sim first — not a script failure.
  </action>
  <verify>test -f .planning/phases/01-entry-baseline/EXPORT-USAGE.md</verify>
  <done>Operator instructions exist</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` passes
- [ ] No secrets logged
</verification>

<success_criteria>
- Export script works on empty or populated DB
- CSV aligns with review-template headers
</success_criteria>

<output>
Create `.planning/phases/01-entry-baseline/01-04-SUMMARY.md` after execution.
</output>
