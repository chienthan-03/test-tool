---
phase: 08-trade-review-workflow
plan: 03
type: execute
depends_on: ["08-02"]
files_modified:
  - src/core/types.ts
  - src/strategy/entry-gate.ts
  - src/execution/sim-broker.ts
  - src/execution/backtest-replayer.ts
  - src/config/schema.ts
  - config/default.yaml
domain: node-trading-bot
---

<objective>
Enrich trade records: exit reason (SL/TP), optional gate reject capture in backtest reports.

Output: `exitReason` on closes; `gateRejects` in report when `entryGates.captureRejects: true`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Enrichment</name>
  <files>src/core/types.ts, src/strategy/entry-gate.ts, src/execution/sim-broker.ts, src/execution/backtest-replayer.ts, src/config/schema.ts</files>
  <action>
- `PositionClosedEvent.exitReason`: SL | TP
- `BacktestTradeRecord`: exitReason, stopLoss, takeProfit
- `GateRejectEvent` + `BacktestReport.gateRejects`
- `entryGates.captureRejects` (default false); EntryGate emits `strategy:gateReject` on bus
- Backtest collects rejects into report JSON
  </action>
  <verify>npm run lint && npm test -- tests/unit/entry-gate.test.ts</verify>
  <done>Reports can include gate rejects and exit reason</done>
</task>

</tasks>

<output>Create `08-03-SUMMARY.md` after execution.</output>
