---
phase: 08-trade-review-workflow
plan: 02
type: execute
depends_on: ["08-01"]
files_modified:
  - scripts/lib/trade-review-csv.ts
  - scripts/export-backtest-trades-review.ts
  - scripts/export-trades-review.ts
  - package.json
domain: node-trading-bot
---

<objective>
Unify backtest and SQLite trade exports to the same review CSV schema.

Output: Shared headers module + `npm run export-trade-review` wrapper.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Shared CSV module and exports</name>
  <files>scripts/lib/trade-review-csv.ts, scripts/export-backtest-trades-review.ts, scripts/export-trades-review.ts, scripts/export-trade-review.ts, package.json</files>
  <action>
- `scripts/lib/trade-review-csv.ts` — `TRADE_REVIEW_HEADERS`, `escapeCsv`, row builders
- Align `export-backtest-trades` columns with Phase 1 template (add exit_reason, stop_loss, take_profit when available)
- `export-trade-review.ts` — `--source backtest|sqlite` routing
- npm script `export-trade-review`
  </action>
  <verify>npm run lint</verify>
  <done>Both sources export identical headers</done>
</task>

</tasks>

<output>Create `08-02-SUMMARY.md` after execution.</output>
