---
phase: 02-backtest-experiments
plan: 04
type: execute
depends_on: ["02-03"]
files_modified:
  - .planning/phases/02-backtest-experiments/EXPERIMENT-PROTOCOL.md
domain: node-trading-bot
---

<objective>
Document how Phases 3–6 run experiments using the harness.

Purpose: Prevents ad-hoc config tweaking; standardizes win-rate comparisons.
Output: `EXPERIMENT-PROTOCOL.md`
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md
@config/experiments/matrix.yaml
@.planning/phases/01-entry-baseline/01-METRICS-SCHEMA.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write experiment protocol</name>
  <files>.planning/phases/02-backtest-experiments/EXPERIMENT-PROTOCOL.md</files>
  <action>
Create `EXPERIMENT-PROTOCOL.md` with:

1. **When to use mock vs real signals**
2. **Steps:** add preset YAML → add entry to matrix.yaml → `npm run backtest-matrix` → read COMPARISON.md → manual review sample trades (export script)
3. **Fixed window:** default 2024-10-01 to 2024-12-31 unless documented change
4. **Naming:** `runId` kebab-case describes variable changed
5. **Acceptance:** winRate + `would_take_again` (human) — not PnL alone
6. **Phase mapping:** Phase 3 = sentiment presets; Phase 4 = strategy.* overrides
7. **Anti-patterns:** changing date range between compared runs; comparing mock to real signals

Reference commands and file paths literally.
  </action>
  <verify>wc -l .planning/phases/02-backtest-experiments/EXPERIMENT-PROTOCOL.md | awk '{print $1>=40}'</verify>
  <done>Protocol ≥40 lines, actionable</done>
</task>

<task type="auto">
  <name>Task 2: Link protocol in PROJECT and Phase 1 notes</name>
  <files>.planning/PROJECT.md</files>
  <action>
Add one bullet under Active requirements in PROJECT.md:
- [ ] Use experiment protocol (`EXPERIMENT-PROTOCOL.md`) for all filter comparisons

Or add "See also" in `01-BASELINE-NOTES.md` pointing to protocol — minimal edit only (1-2 lines), do not rewrite PROJECT.md.
  </action>
  <verify>grep -q "EXPERIMENT-PROTOCOL" .planning/PROJECT.md .planning/phases/01-entry-baseline/01-BASELINE-NOTES.md 2>/dev/null | head -1</verify>
  <done>Cross-link exists</done>
</task>

</tasks>

<verification>
- [ ] Protocol references experiments-index.json format
</verification>

<success_criteria>
- Third party can run Phase 3 grid without guessing workflow
</success_criteria>

<output>
Create `02-04-SUMMARY.md` after execution.
</output>
