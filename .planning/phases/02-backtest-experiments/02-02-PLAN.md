---
phase: 02-backtest-experiments
plan: 02
type: execute
depends_on: ["02-01"]
files_modified:
  - config/experiments/baseline-mock.yaml
  - config/experiments/stricter-min-strength.yaml
  - config/experiments/matrix.yaml
  - scripts/run-backtest-matrix.ts
  - package.json
domain: node-trading-bot
---

<objective>
Add experiment config presets and a matrix runner that executes multiple backtests from one manifest.

Purpose: Repeatable comparisons for Phases 3–4 filter research.
Output: `config/experiments/*`, `scripts/run-backtest-matrix.ts`, npm script.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md
@config/default.yaml
@src/config/loader.ts
@src/execution/backtest-replayer.ts
@.planning/phases/01-entry-baseline/baseline-backtest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create experiment config presets</name>
  <files>config/experiments/baseline-mock.yaml, config/experiments/stricter-min-strength.yaml, config/experiments/matrix.yaml</files>
  <action>
Create `config/experiments/`:

1. `baseline-mock.yaml` — copy `config/default.yaml` (or document extends default); add comment header `# experiment: baseline-mock`

2. `stricter-min-strength.yaml` — same as default but `sentiment.rules.minStrength: 0.65` (or +0.15 vs default 0.5) and comment explaining hypothesis

3. `matrix.yaml` manifest:
```yaml
from: "2024-10-01"
to: "2024-12-31"
mockSentiment: true
runs:
  - id: baseline-mock
    config: config/experiments/baseline-mock.yaml
  - id: stricter-min-strength
    config: config/experiments/stricter-min-strength.yaml
```

Use dates matching Phase 1 baseline. Do not break Zod schema — only override valid fields.
  </action>
  <verify>npm run lint && test -f config/experiments/matrix.yaml</verify>
  <done>3 experiment files exist and parse via loadConfig</done>
</task>

<task type="auto">
  <name>Task 2: Implement run-backtest-matrix script</name>
  <files>scripts/run-backtest-matrix.ts, package.json</files>
  <action>
Create `scripts/run-backtest-matrix.ts`:

- Args: `--matrix config/experiments/matrix.yaml` (required), optional `--dry-run`
- Parse matrix YAML (use `yaml` package like loader)
- For each run: `loadConfig`, `openDatabase`, `migrate`, `runBacktest({ config, db, from, to, symbols: config.symbols, mockSentiment: matrix.mockSentiment })`
- Use temp SQLite per run under `data/reports/experiments/.tmp/` or in-memory path in matrix
- **Do not** re-download klines if cache exists (`skipDownload: true` on replayer if option exists — check `BacktestReplayerOptions`)

Add npm script: `"backtest-matrix": "tsx scripts/run-backtest-matrix.ts"`

Log progress to stderr; failures for one run should not abort entire matrix (collect errors).

Dry-run: print planned runs only.
  </action>
  <verify>npm run lint && npm run backtest-matrix -- --matrix config/experiments/matrix.yaml --dry-run</verify>
  <done>Dry-run lists 2 runs; script compiles</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` passes
- [ ] Presets are valid AppConfig
</verification>

<success_criteria>
- Matrix runner can invoke backtest for each preset
</success_criteria>

<output>
Create `02-02-SUMMARY.md` after execution.
</output>
