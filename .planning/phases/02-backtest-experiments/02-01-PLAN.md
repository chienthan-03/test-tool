---
phase: 02-backtest-experiments
plan: 01
type: execute
depends_on: []
files_modified:
  - .planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md
domain: node-trading-bot
---

<objective>
Inventory current backtest capabilities and gaps vs Phase 2 goals (repeatable config matrix).

Purpose: Ground implementation in facts from Phase 1 baseline.
Output: `BACKTEST-INVENTORY.md`
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-entry-baseline/01-BASELINE-NOTES.md
@.planning/phases/01-entry-baseline/baseline-backtest-notes.md
@src/cli/commands/backtest.ts
@src/execution/backtest-replayer.ts
@src/core/types.ts
@config/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document existing backtest I/O</name>
  <files>.planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md</files>
  <action>
Create `BACKTEST-INVENTORY.md` section "Current capabilities":

- CLI: `npm run dev -- backtest --from --to --config [--mock-sentiment]`
- Output: stdout JSON (6 metrics) + full report file `data/reports/backtest-{timestamp}.json` (includes `trades[]`)
- Klines: `data/klines/` cache, download via `kline-cache.ts`
- Config: single YAML path, Zod validation
- Signals: DB `news_signals` OR `--mock-sentiment`
- Phase 1 baseline: `baseline-backtest.json` manual capture

List file paths and `BacktestReport` fields from `src/core/types.ts`.
  </action>
  <verify>grep -q "BacktestReport" .planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md</verify>
  <done>Capabilities section complete</done>
</task>

<task type="auto">
  <name>Task 2: Document gaps and Phase 2 requirements</name>
  <files>.planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md</files>
  <action>
Append sections:

**Gaps (today):**
- No labeled experiment runs (only timestamp filenames)
- No config hash / preset name in report metadata
- No matrix runner (manual one-off commands)
- No comparison index across runs
- stdout-only workflow in Phase 1 baseline
- Trades not in SQLite (optional enhancement — mark as nice-to-have)

**Phase 2 must deliver:**
- Preset configs under `config/experiments/`
- `scripts/run-backtest-matrix.ts` (or npm script)
- `data/reports/experiments/` structured output + `experiments-index.json`
- Protocol doc for Phases 3–4

**Non-goals this phase:** RSS replay, LLM in backtest loop, live parity
  </action>
  <verify>grep -q "Gaps" .planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md && grep -q "experiments-index" .planning/phases/02-backtest-experiments/BACKTEST-INVENTORY.md</verify>
  <done>Gaps mapped to plans 02-02 through 02-04</done>
</task>

</tasks>

<verification>
- [ ] Aligns with Phase 1 `01-BASELINE-NOTES.md` hypotheses
</verification>

<success_criteria>
- Inventory is actionable input for matrix script design
</success_criteria>

<output>
Create `02-01-SUMMARY.md` after execution.
</output>
