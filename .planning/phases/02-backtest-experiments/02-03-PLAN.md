---
phase: 02-backtest-experiments
plan: 03
type: execute
depends_on: ["02-02"]
files_modified:
  - scripts/run-backtest-matrix.ts
  - src/execution/backtest-replayer.ts
  - data/reports/experiments/.gitkeep
domain: node-trading-bot
---

<objective>
Persist labeled per-run artifacts and build `experiments-index.json` for side-by-side comparison.

Purpose: Phase 3/4 need diffable run history, not timestamp-only files.
Output: Structured `data/reports/experiments/{runId}/` + index file.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/run-backtest-matrix.ts
@src/execution/backtest-replayer.ts
@.planning/phases/01-entry-baseline/01-METRICS-SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structured experiment output dirs</name>
  <files>scripts/run-backtest-matrix.ts, data/reports/experiments/.gitkeep</files>
  <action>
Extend matrix runner (or helper) to write per run:

`data/reports/experiments/{runId}/`
- `report.json` — full BacktestReport
- `meta.json` — `{ runId, configPath, configSha256 (hash file contents), from, to, mockSentiment, executedAt }`
- `summary.json` — metrics only (same 6 fields as CLI stdout)

After all runs, write `data/reports/experiments/experiments-index.json`:
```json
{ "matrix": "config/experiments/matrix.yaml", "runs": [ { "id", "winRate", "totalTrades", ... } ] }
```

Add `data/reports/experiments/` to `.gitignore` for `*.json` reports OR commit `.gitkeep` only — prefer gitignore `data/reports/experiments/**/report.json` but keep index optional.

Use Node `crypto.createHash('sha256')` for config hash.

Optionally extend `backtest-replayer.ts` to accept optional `reportPath` override — only if needed; prefer matrix script copying/writing report after run to minimize core changes.
  </action>
  <verify>npm run backtest-matrix -- --matrix config/experiments/matrix.yaml 2>&1 | tail -5</verify>
  <done>experiments-index.json exists with ≥2 runs after full matrix execution</done>
</task>

<task type="auto">
  <name>Task 2: Generate comparison markdown</name>
  <files>scripts/run-backtest-matrix.ts</files>
  <action>
After index written, generate `data/reports/experiments/COMPARISON.md` table:

| runId | totalTrades | winRate | totalPnlUsdt | maxDrawdownPct | config |

Sort by winRate descending. Include note that mock-sentiment runs ignore sentiment preset diffs until real signals (stricter-min-strength may match baseline under mock — document that observation).

Print path to COMPARISON.md on stderr when matrix completes.
  </action>
  <verify>test -f data/reports/experiments/COMPARISON.md || echo "created on run"</verify>
  <done>COMPARISON.md generated on successful matrix run</done>
</task>

</tasks>

<verification>
- [ ] Full matrix completes in &lt;5 min on cached klines
- [ ] Index lists all run ids from matrix.yaml
</verification>

<success_criteria>
- Operator can compare runs without manual JSON hunting
</success_criteria>

<output>
Create `02-03-SUMMARY.md` after execution.
</output>
