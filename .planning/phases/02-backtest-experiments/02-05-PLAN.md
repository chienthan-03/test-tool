---
phase: 02-backtest-experiments
plan: 05
type: execute
depends_on: ["02-02"]
files_modified:
  - tests/unit/backtest-matrix.test.ts
  - tests/integration/backtest-matrix-smoke.test.ts
domain: node-trading-bot
---

<objective>
Add unit and smoke tests for matrix manifest parsing and dry-run behavior.

Purpose: Prevent regressions in experiment harness before Phase 3 relies on it.
Output: 2 test files passing in `npm test`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/references/tdd.md
</execution_context>

<context>
@scripts/run-backtest-matrix.ts
@config/experiments/matrix.yaml
@tests/integration/backtest-smoke.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit test matrix manifest parsing</name>
  <files>tests/unit/backtest-matrix.test.ts</files>
  <action>
Export parse function from matrix script (e.g. `parseMatrixManifest(path)`) OR test via reading same YAML shape in test.

Test cases:
- Valid matrix.yaml returns 2 runs with ids and config paths
- Missing `runs` throws or returns error
- Invalid date format rejected

Keep tests fast — no backtest execution.
  </action>
  <verify>npm test -- tests/unit/backtest-matrix.test.ts</verify>
  <done>Unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Integration smoke with synthetic klines</name>
  <files>tests/integration/backtest-matrix-smoke.test.ts</files>
  <action>
Integration test pattern like `backtest-smoke.test.ts`:
- mkdtemp cache + reports dirs
- Write minimal matrix with 1 run, mockSentiment true, 1 symbol BTCUSDT
- Use `writeSyntheticKlines` for context+entry timeframes
- Run matrix programmatically (import runner function, not shell)
- Assert `experiments-index.json` has 1 entry and winRate is number

Timeout reasonable (&lt;30s).
  </action>
  <verify>npm test -- tests/integration/backtest-matrix-smoke.test.ts</verify>
  <done>Smoke test passes</done>
</task>

</tasks>

<verification>
- [ ] `npm test` full suite still passes
</verification>

<success_criteria>
- Harness covered by automated tests
</success_criteria>

<output>
Create `02-05-SUMMARY.md` after execution.
</output>
