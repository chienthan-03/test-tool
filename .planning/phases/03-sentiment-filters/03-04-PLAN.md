---
phase: 03-sentiment-filters
plan: 04
type: execute
depends_on: ["03-02"]
files_modified:
  - config/experiments/sentiment-llm-on.yaml
  - config/experiments/sentiment-matrix-llm.yaml
  - .planning/phases/03-sentiment-filters/LLM-COMPARISON.md
domain: node-trading-bot
---

<objective>
Compare rule-only vs LLM-enabled sentiment paths on same fixtures (if OPENROUTER_API_KEY available).

Purpose: Quantify whether LLM improves win rate enough to justify cost/latency.
Output: `LLM-COMPARISON.md` + optional matrix run results.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@config/experiments/sentiment-no-llm.yaml
@src/sentiment/llm-gateway.ts
@.planning/phases/02-backtest-experiments/EXPERIMENT-PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLM on/off matrix</name>
  <files>config/experiments/sentiment-matrix-llm.yaml, config/experiments/sentiment-llm-on.yaml</files>
  <action>
Create `sentiment-llm-on.yaml` (baseline with `llm.enabled: true`) vs existing `sentiment-no-llm.yaml`.

Create minimal `sentiment-matrix-llm.yaml` with 2 runs, `mockSentiment: false`, same date window.

If `OPENROUTER_API_KEY` missing: document in `LLM-COMPARISON.md "Skipped — no API key"` and compare rule-only presets only (still complete plan).

If key present: run seed + matrix for both; note LLM call count limit (`maxCallsPerHour`) may truncate — document.

Do NOT commit API keys.
  </action>
  <verify>test -f .planning/phases/03-sentiment-filters/LLM-COMPARISON.md</verify>
  <done>LLM comparison documented (executed or skipped with reason)</done>
</task>

<task type="auto">
  <name>Task 2: Summarize LLM tradeoff</name>
  <files>.planning/phases/03-sentiment-filters/LLM-COMPARISON.md</files>
  <action>
`LLM-COMPARISON.md` sections:
- Setup (configs, fixtures, key present y/n)
- Metrics table (winRate, trades, pnl)
- Recommendation: enable LLM for production? (yes/no/conditional)
- Link to Phase 6 for implementation

If skipped: recommend Phase 3 default remains rule-only until key available.
  </action>
  <verify>grep -q "Recommendation" .planning/phases/03-sentiment-filters/LLM-COMPARISON.md</verify>
  <done>Clear recommendation paragraph exists</done>
</task>

</tasks>

<success_criteria>
- LLM path evaluated or explicitly deferred with rationale
</success_criteria>

<output>
Create `03-04-SUMMARY.md` after execution.
</output>
