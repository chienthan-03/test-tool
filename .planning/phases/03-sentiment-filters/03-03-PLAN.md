---
phase: 03-sentiment-filters
plan: 03
type: execute
depends_on: ["03-02"]
files_modified:
  - .planning/phases/03-sentiment-filters/FALSE-SIGNAL-ANALYSIS.md
  - scripts/export-signals-review.ts
domain: node-trading-bot
---

<objective>
Analyze discarded and weak signals from fixture replay; sample for manual review.

Purpose: Identify false-positive patterns to guide keyword/blacklist/tag rule changes.
Output: `FALSE-SIGNAL-ANALYSIS.md` + optional `signals-review.csv`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-entry-baseline/01-ENTRY-PATH.md
@.planning/phases/01-entry-baseline/review-template.csv
@src/sentiment/rule-scorer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export signal-level audit from seed run</name>
  <files>scripts/export-signals-review.ts</files>
  <action>
Extend seed script OR add `scripts/export-signals-review.ts` querying:
- `news` table (if populated) + `news_signals` with join on news_id
- Export CSV: title, symbols, direction, strength, source (rule/llm/merged), impactScore if stored

For discarded items: log during seed to `data/reports/experiments/sentiment-phase3/discards.jsonl` (reason: blacklist, minStrength, neutral sentiment, no symbols).

Analyze discards in `FALSE-SIGNAL-ANALYSIS.md`:
- Count by discard reason
- Top 5 example titles per reason (from fixtures)
- Recommend 2–3 rule tweaks (not implemented yet — Phase 6)
  </action>
  <verify>test -f .planning/phases/03-sentiment-filters/FALSE-SIGNAL-ANALYSIS.md</verify>
  <done>Analysis has discard breakdown + examples</done>
</task>

<task type="auto">
  <name>Task 2: Manual review sample template</name>
  <files>.planning/phases/03-sentiment-filters/signals-review-sample.csv</files>
  <action>
Create `signals-review-sample.csv` with 10–15 emitted signals from baseline seed run.

Columns: title, direction, strength, source, would_trade (empty), notes (empty).

Add section in FALSE-SIGNAL-ANALYSIS.md "Manual review instructions" linking `01-ENTRY-PATH.md` gates 1–3.
  </action>
  <verify>wc -l .planning/phases/03-sentiment-filters/signals-review-sample.csv</verify>
  <done>Sample CSV ready for human fill-in</done>
</task>

</tasks>

<success_criteria>
- False signal patterns documented with evidence
</success_criteria>

<output>
Create `03-03-SUMMARY.md` after execution.
</output>
