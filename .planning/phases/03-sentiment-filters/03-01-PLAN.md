---
phase: 03-sentiment-filters
plan: 01
type: execute
depends_on: []
files_modified:
  - .planning/phases/03-sentiment-filters/SENTIMENT-GRID.md
  - config/experiments/sentiment-matrix.yaml
  - config/experiments/sentiment-baseline.yaml
  - config/experiments/sentiment-high-min-strength.yaml
  - config/experiments/sentiment-high-threshold-llm.yaml
  - config/experiments/sentiment-no-llm.yaml
domain: node-trading-bot
---

<objective>
Define the sentiment experiment grid and signal seeding strategy so backtests use real rule/merger paths (not mock sentiment).

Purpose: Phase 2 proved mock sentiment ignores `minStrength`; Phase 3 must use `news_signals` in SQLite.
Output: `SENTIMENT-GRID.md` + `config/experiments/sentiment-*.yaml` + `sentiment-matrix.yaml`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-experiments/EXPERIMENT-PROTOCOL.md
@.planning/phases/01-entry-baseline/baseline-backtest.json
@src/sentiment/rule-scorer.ts
@src/sentiment/signal-merger.ts
@src/sentiment/news-pipeline.ts
@tests/fixtures/rss/coindesk-sample.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document grid and hypotheses</name>
  <files>.planning/phases/03-sentiment-filters/SENTIMENT-GRID.md</files>
  <action>
Create `SENTIMENT-GRID.md` table:

| runId | Variable | Hypothesis |
|-------|----------|------------|
| sentiment-baseline | default rules | Reference |
| sentiment-high-min-strength | minStrength 0.65 | Fewer weak signals |
| sentiment-high-threshold-llm | thresholdLLM 4 | Less LLM, more rule-only |
| sentiment-no-llm | llm.enabled false | Rule-only path |

Document **signal seeding plan** (Plan 03-02 implements):
- Replay `tests/fixtures/rss/*.xml` through `NewsPipeline` into experiment DB
- OR use `npm run dev -- validate` / short sim poll — pick fixture replay for reproducibility
- Signals must have `createdAt` within 2024-10-01–2024-12-31 (distribute fixture items across window)

Fixed backtest window: same as Phase 1/2.
  </action>
  <verify>test -f .planning/phases/03-sentiment-filters/SENTIMENT-GRID.md</verify>
  <done>Grid has ≥4 runs with one variable each</done>
</task>

<task type="auto">
  <name>Task 2: Create sentiment experiment configs and matrix</name>
  <files>config/experiments/sentiment-baseline.yaml, config/experiments/sentiment-high-min-strength.yaml, config/experiments/sentiment-high-threshold-llm.yaml, config/experiments/sentiment-no-llm.yaml, config/experiments/sentiment-matrix.yaml</files>
  <action>
Copy `config/experiments/baseline-mock.yaml` as base for each preset (or `config/default.yaml`), apply single change per file:

- `sentiment-baseline.yaml` — same as current production rules
- `sentiment-high-min-strength.yaml` — `minStrength: 0.65`
- `sentiment-high-threshold-llm.yaml` — `thresholdLLM: 4` (was 3)
- `sentiment-no-llm.yaml` — `llm.enabled: false`

Create `sentiment-matrix.yaml`:
```yaml
from: "2024-10-01"
to: "2024-12-31"
mockSentiment: false
experimentsDir: ./data/reports/experiments/sentiment-phase3
signalDbPath: ./data/reports/experiments/sentiment-phase3/signals.db  # optional key for runner extension
runs: [ four entries ]
```

Note: `mockSentiment: false` — critical for this phase.
  </action>
  <verify>npm run lint && npm run backtest-matrix -- --matrix config/experiments/sentiment-matrix.yaml --dry-run</verify>
  <done>4 presets + matrix dry-run lists runs</done>
</task>

</tasks>

<verification>
- [ ] Each preset differs in exactly one sentiment field from baseline
</verification>

<success_criteria>
- Reproducible sentiment comparison matrix defined
</success_criteria>

<output>
Create `03-01-SUMMARY.md` after execution.
</output>
