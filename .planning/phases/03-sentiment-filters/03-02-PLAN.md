---
phase: 03-sentiment-filters
plan: 02
type: execute
depends_on: ["03-01"]
files_modified:
  - scripts/seed-signals-from-fixtures.ts
  - scripts/run-backtest-matrix.ts
  - package.json
domain: node-trading-bot
---

<objective>
Seed `news_signals` from RSS fixtures and run sentiment matrix with `mockSentiment: false`.

Purpose: Produce comparable backtest metrics where sentiment gates actually differ.
Output: `data/reports/experiments/sentiment-phase3/` results per preset.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-sentiment-filters/SENTIMENT-GRID.md
@config/experiments/sentiment-matrix.yaml
@scripts/run-backtest-matrix.ts
@tests/integration/rss-pipeline.test.ts
@tests/fixtures/rss/coindesk-sample.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture-to-signals seed script</name>
  <files>scripts/seed-signals-from-fixtures.ts, package.json</files>
  <action>
Create `scripts/seed-signals-from-fixtures.ts`:

- Args: `--config`, `--db` (output sqlite path), `--from`, `--to`
- Load RSS XML from `tests/fixtures/rss/` (coindesk-sample.xml, macro-ambiguous.xml, doge-only.xml — process all)
- Run through full pipeline: SymbolMapper, NewsPipeline (RuleScorer, SignalMerger, llm disabled or mock to avoid API)
- For LLM-off research path in 03-04, support `--no-llm` flag forcing llmGateway null
- Assign `createdAt` spread evenly between from/to so `listBetween` picks them up
- Print counts: items processed, signals inserted, discarded

npm script: `"seed-signals": "tsx scripts/seed-signals-from-fixtures.ts"`

Default db: `data/reports/experiments/sentiment-phase3/signals.db`

**Extend** `run-backtest-matrix.ts` (minimal): optional matrix field `signalDbPath` — if set, use that db for all runs instead of per-run temp db (signals regenerated per config in 03-02 by re-seeding before each run OR seed once per config in loop).

Preferred approach: **re-seed per run** inside matrix loop when `signalDbPath` template includes `{runId}` OR call seed script before each backtest with that run's config.

Document in script header.
  </action>
  <verify>npm run lint && npm run seed-signals -- --config config/experiments/sentiment-baseline.yaml --db /tmp/test-signals.db --from 2024-10-01 --to 2024-12-31 --no-llm 2>&1 | tail -3</verify>
  <done>Seed script inserts ≥1 signal into test db</done>
</task>

<task type="auto">
  <name>Task 2: Run sentiment matrix</name>
  <files>data/reports/experiments/sentiment-phase3/experiments-index.json</files>
  <action>
For each run in `sentiment-matrix.yaml`:
1. Seed signals with that run's config into `data/reports/experiments/sentiment-phase3/{runId}-signals.db` (or single db cleared and re-seeded)
2. Run backtest with `mockSentiment: false` and that signal db

Execute:
```bash
npm run backtest-matrix -- --matrix config/experiments/sentiment-matrix.yaml
```

After completion, copy `experiments-index.json` summary into `.planning/phases/03-sentiment-filters/sentiment-matrix-results.json`.

If zero signals for a run, document in SUMMARY — fix seed script, do not skip silently.
  </action>
  <verify>test -f data/reports/experiments/sentiment-phase3/experiments-index.json</verify>
  <done>Index has 4 runs with metrics; at least one run has totalTrades &gt; 0</done>
</task>

</tasks>

<verification>
- [ ] `mockSentiment: false` confirmed in matrix yaml
- [ ] COMPARISON.md shows differences OR documents why identical (fixture limits)
</verification>

<success_criteria>
- Sentiment presets produce measurable backtest diffs or documented blocker
</success_criteria>

<output>
Create `03-02-SUMMARY.md` after execution.
</output>
