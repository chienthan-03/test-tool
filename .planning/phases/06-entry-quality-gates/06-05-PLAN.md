---
phase: 06-entry-quality-gates
plan: 05
type: execute
depends_on: ["06-01", "06-04"]
files_modified:
  - tests/unit/entry-gate.test.ts
  - tests/unit/mtf-engine.test.ts
domain: node-trading-bot
---

<objective>
Unit tests for entry gate veto paths and tighter Fib tolerance behavior.

Purpose: Lock research parameters with regression tests.
Output: `tests/unit/entry-gate.test.ts` (+ extend mtf tests if needed).
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/strategy/entry-gate.ts
@src/strategy/mtf-engine.ts
@tests/unit/mtf-engine.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: EntryGate unit tests</name>
  <files>tests/unit/entry-gate.test.ts</files>
  <action>
Test `EntryGate` with mocked or minimal `KlineStore` + canned candles (reuse patterns from mtf-engine tests if any):

- Returns `allow: false` with `stage: 'context'` when context blocks
- Returns `allow: false` with `stage: 'entry'` when outside fib zone
- Returns `allow: true` with entry SL/TP when setup valid
- `entryGates.enabled: false` bypasses checks (if implemented on EntryGate)

Add test: `zoneTolerancePercent: 0.02` rejects price that passes at `0.05` (if not already in mtf-engine tests).
  </action>
  <verify>npm test -- tests/unit/entry-gate.test.ts tests/unit/mtf-engine.test.ts</verify>
  <done>Unit tests pass</done>
</task>

</tasks>

<success_criteria>
- Gate veto reasons covered by tests
</success_criteria>

<output>
Create `06-05-SUMMARY.md` after execution.
</output>
