---
phase: 06-entry-quality-gates
plan: 01
type: execute
depends_on: []
files_modified:
  - .planning/phases/06-entry-quality-gates/ENTRY-GATES-DESIGN.md
  - src/strategy/entry-gate.ts
  - src/strategy/strategy-engine.ts
domain: node-trading-bot
---

<objective>
Design a single entry-gate layer that evaluates MTF context/entry before `strategy:intent` and records reject reasons.

Purpose: Today MTF failures return silently in `StrategyEngine`; Phase 6 needs one veto point and optional logging for debugging/review.
Output: `ENTRY-GATES-DESIGN.md` + `entry-gate.ts` wired into `strategy-engine.ts`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-entry-baseline/01-ENTRY-PATH.md
@.planning/phases/04-mtf-entry-alignment/MTF-ENTRY-RULES.md
@src/strategy/strategy-engine.ts
@src/strategy/mtf-engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design document</name>
  <files>.planning/phases/06-entry-quality-gates/ENTRY-GATES-DESIGN.md</files>
  <action>
Create `ENTRY-GATES-DESIGN.md`:

1. **Scope** — Phase 6 gates = MTF technical path (context + entry). Sentiment gates remain in `NewsPipeline` / `SignalMerger` (config merge in 06-02).
2. **Interface** — `EntryGateResult { allow: boolean; reason?: string; stage: 'context' | 'entry' }`
3. **Flow** — `StrategyEngine.handleCandleClose` calls `EntryGate.evaluate(symbol, direction, strength)` instead of inline `mtf.evaluateContext/Entry`
4. **Logging** — optional debug log on reject when `config.logging.level === 'debug'` or new `entryGates.logRejects`
5. **Out of scope Phase 6** — per-symbol cooldown after loss → Phase 7
6. Diagram: signal → pending → candle → entry gate → intent

No behavior change until 06-03 merges `zoneTolerancePercent: 0.02` into default config.
  </action>
  <verify>test -f .planning/phases/06-entry-quality-gates/ENTRY-GATES-DESIGN.md</verify>
  <done>Design doc approved for implementation tasks</done>
</task>

<task type="auto">
  <name>Task 2: Implement EntryGate wrapper</name>
  <files>src/strategy/entry-gate.ts, src/strategy/strategy-engine.ts</files>
  <action>
Create `src/strategy/entry-gate.ts`:

- `EntryGate` class wrapping `MtfEngine`
- `evaluate(symbol, direction, strength)` → `{ allow, reason?, stage?, entry?: MtfEntryResult }` — on allow, include entry SL/TP/atr from `evaluateEntry`
- Refactor `StrategyEngine` to use `EntryGate` (minimal diff)

Export types for tests.

Run `npm run lint`.
  </action>
  <verify>npm run lint</verify>
  <done>StrategyEngine uses EntryGate; behavior unchanged with current default.yaml</done>
</task>

</tasks>

<success_criteria>
- Single MTF veto point exists with structured reject reasons
</success_criteria>

<output>
Create `06-01-SUMMARY.md` after execution.
</output>
