---
phase: 06-entry-quality-gates
plan: 07
type: execute
depends_on: ["06-03"]
files_modified:
  - config/experiments/phase6-pre-research.yaml
  - config/experiments/phase6-validation-matrix.yaml
  - .planning/phases/06-entry-quality-gates/phase6-validation-results.json
  - .planning/phases/06-entry-quality-gates/PHASE6-VALIDATION.md
domain: node-trading-bot
---

<objective>
Backtest validation: pre-research defaults vs Phase 6 production config on 5-symbol universe.

Purpose: Confirm combined merges improve or hold metrics vs Phase 5 expanded baseline.
Output: `PHASE6-VALIDATION.md` + matrix results.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-symbol-expansion/SYMBOL-BACKTEST-COMPARISON.md
@.planning/phases/04-mtf-entry-alignment/mtf-matrix-results.json
@config/experiments/symbols-expanded.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validation matrix configs</name>
  <files>config/experiments/phase6-pre-research.yaml, config/experiments/phase6-validation-matrix.yaml</files>
  <action>
`phase6-pre-research.yaml` — 5 symbols, `llm.enabled: true`, `zoneTolerancePercent: 0.05` (pre-Phase 3/4 merges); all else matches current defaults.

`phase6-validation-matrix.yaml`:
```yaml
mockSentiment: true
experimentsDir: ./data/reports/experiments/phase6-validation
runs:
  - id: pre-research
    config: config/experiments/phase6-pre-research.yaml
  - id: phase6-production
    config: config/experiments/phase6-production.yaml
```

Run matrix after klines prefetched.
  </action>
  <verify>npm run backtest-matrix -- --matrix config/experiments/phase6-validation-matrix.yaml</verify>
  <done>Two-run comparison complete</done>
</task>

<task type="auto">
  <name>Task 2: Validation report</name>
  <files>.planning/phases/06-entry-quality-gates/PHASE6-VALIDATION.md, .planning/phases/06-entry-quality-gates/phase6-validation-results.json</files>
  <action>
`PHASE6-VALIDATION.md`:

- Metrics table
- Compare to Phase 5 `symbols-expanded` (47 trades, 25.5% — note that run used mtf-recommended already; pre-research should differ on fib + llm flags)
- Success criteria: phase6-production winRate ≥ pre-research OR fewer trades with higher winRate
- Optional: second run with `seedFromFixtures` + `mockSentiment: false` documented as stretch goal

Copy index to `phase6-validation-results.json`.
  </action>
  <verify>test -f .planning/phases/06-entry-quality-gates/PHASE6-VALIDATION.md</verify>
  <done>Validation documented</done>
</task>

</tasks>

<success_criteria>
- Quantified before/after for Phase 6 config merge
</success_criteria>

<output>
Create `06-07-SUMMARY.md` after execution.
</output>
