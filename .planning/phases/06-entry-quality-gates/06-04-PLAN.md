---
phase: 06-entry-quality-gates
plan: 04
type: execute
depends_on: ["06-01"]
files_modified:
  - src/config/schema.ts
  - config/default.yaml
  - .planning/phases/06-entry-quality-gates/GATES-CONFIG.md
domain: node-trading-bot
---

<objective>
Expose entry-gate settings in YAML + Zod for operator tuning without code changes.

Purpose: Centralize logging/toggle for gate layer; document where sentiment/MTF knobs live.
Output: `entryGates` config section + `GATES-CONFIG.md`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/config/schema.ts
@.planning/phases/06-entry-quality-gates/ENTRY-GATES-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema and default YAML</name>
  <files>src/config/schema.ts, config/default.yaml</files>
  <action>
Add to `AppConfigSchema`:

```yaml
entryGates:
  logRejects: true   # info-level log when MTF gate blocks entry
  enabled: true      # if false, skip gate (escape hatch — use only in tests)
```

Wire `EntryGate` / `StrategyEngine` to respect `entryGates.enabled` and log rejects when `logRejects` true (use existing logger).

Defaults: `enabled: true`, `logRejects: false` (avoid noisy prod logs).

Update experiment YAMLs that copy default structure if loader validates full schema on experiments — at minimum `phase6-production.yaml`.
  </action>
  <verify>npm run lint</verify>
  <done>Schema loads; entryGates present</done>
</task>

<task type="auto">
  <name>Task 2: Config reference doc</name>
  <files>.planning/phases/06-entry-quality-gates/GATES-CONFIG.md</files>
  <action>
`GATES-CONFIG.md` — table mapping:

| Concern | Config path |
|---------|-------------|
| Sentiment min strength | `sentiment.rules.minStrength` |
| LLM on/off | `sentiment.llm.enabled` |
| Fib zone | `strategy.fibonacci.zoneTolerancePercent` |
| MTF gate logging | `entryGates.logRejects` |
| Cooldown (future) | Phase 7 — not in schema yet |

Note: sentiment/MTF values are the real "gates"; `entryGates` controls observability.
  </action>
  <verify>test -f .planning/phases/06-entry-quality-gates/GATES-CONFIG.md</verify>
  <done>Operator reference complete</done>
</task>

</tasks>

<success_criteria>
- Gate observability configurable via YAML
</success_criteria>

<output>
Create `06-04-SUMMARY.md` after execution.
</output>
