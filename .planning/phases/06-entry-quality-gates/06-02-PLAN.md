---
phase: 06-entry-quality-gates
plan: 02
type: execute
depends_on: ["06-01"]
files_modified:
  - config/default.yaml
  - config/experiments/phase6-production.yaml
  - .planning/phases/06-entry-quality-gates/SENTIMENT-MERGE.md
domain: node-trading-bot
---

<objective>
Merge Phase 3 sentiment preset into production config (`llm.enabled: false`).

Purpose: Research chose rule-only path; production should match without separate experiment files.
Output: Updated `default.yaml` + `phase6-production.yaml` stub + merge doc.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-sentiment-filters/SENTIMENT-FINDINGS-FOR-PHASE6.md
@config/experiments/sentiment-recommended.yaml
@config/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge sentiment into default.yaml</name>
  <files>config/default.yaml</files>
  <action>
Set `sentiment.llm.enabled: false` in `config/default.yaml`.

Keep `minStrength: 0.5`, `thresholdLLM: 3` unchanged (per Phase 3 recommendation).

Verify `NewsPipeline` skips LLM when disabled (existing behavior — no code change unless broken).
  </action>
  <verify>npx tsx -e "import { loadConfig } from './src/config/loader.js'; console.log(loadConfig('config/default.yaml').sentiment.llm.enabled)"</verify>
  <done>default.yaml has llm.enabled false</done>
</task>

<task type="auto">
  <name>Task 2: Document merge</name>
  <files>.planning/phases/06-entry-quality-gates/SENTIMENT-MERGE.md, config/experiments/phase6-production.yaml</files>
  <action>
`SENTIMENT-MERGE.md` — diff table vs pre-Phase-6 default.

Create `config/experiments/phase6-production.yaml` — copy current `default.yaml` after merges (updated again in 06-03) for matrix validation runs.
  </action>
  <verify>test -f config/experiments/phase6-production.yaml</verify>
  <done>Merge documented</done>
</task>

</tasks>

<success_criteria>
- Production config reflects Phase 3 sentiment decision
</success_criteria>

<output>
Create `06-02-SUMMARY.md` after execution.
</output>
