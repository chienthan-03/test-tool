---
phase: 06-entry-quality-gates
plan: 06
type: execute
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - tests/integration/entry-gates-intent.test.ts
domain: node-trading-bot
---

<objective>
Integration test: news signal through pending → candle close → `strategy:intent` → `risk:orderPlan` with production gates enabled.

Purpose: Prove end-to-end path still executes when gates pass.
Output: `tests/integration/entry-gates-intent.test.ts`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@tests/integration/rss-pipeline.test.ts
@src/strategy/strategy-engine.ts
@src/risk/risk-engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intent → order plan integration test</name>
  <files>tests/integration/entry-gates-intent.test.ts</files>
  <action>
Create integration test:

- Load `config/experiments/phase6-production.yaml` or `default.yaml`
- Seed `KlineStore` with sufficient candles for BTCUSDT (read from `data/klines/` if present, or synthetic minimal candles that satisfy MTF — may need fixture candles JSON in `tests/fixtures/candles/`)
- Emit `news:signal` on bus, fire `market:candleClose` on entry TF with price in fib zone
- Assert `strategy:intent` and `risk:orderPlan` events emitted (spy on bus)

If full MTF pass is too heavy, document minimal candle fixture and skip with message only as last resort — prefer making it pass.

Run: `npm test -- tests/integration/entry-gates-intent.test.ts`
  </action>
  <verify>npm test -- tests/integration/entry-gates-intent.test.ts</verify>
  <done>Integration test passes or documents fixture gap in SUMMARY</done>
</task>

</tasks>

<success_criteria>
- E2E path from signal to order plan verified under phase6 config
</success_criteria>

<output>
Create `06-06-SUMMARY.md` after execution.
</output>
