---
phase: 04-mtf-entry-alignment
plan: 01
type: execute
depends_on: []
files_modified:
  - .planning/phases/04-mtf-entry-alignment/MTF-ENTRY-RULES.md
domain: node-trading-bot
---

<objective>
Document current MTF entry rules, config knobs, and known gaps vs roadmap intent.

Purpose: Phase 3 showed 60 signals → 2 trades — MTF gates need a single source of truth before experiments.
Output: `MTF-ENTRY-RULES.md` referencing code paths and default.yaml values.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-entry-baseline/01-ENTRY-PATH.md
@src/strategy/mtf-engine.ts
@src/strategy/strategy-engine.ts
@src/strategy/pending-signals.ts
@config/default.yaml
@.planning/phases/01-entry-baseline/baseline-backtest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map context + entry evaluation</name>
  <files>.planning/phases/04-mtf-entry-alignment/MTF-ENTRY-RULES.md</files>
  <action>
Create `MTF-ENTRY-RULES.md` with:

1. **Context gate** (`evaluateContext`) — Elliott trend, impulse, sideways/strongNews, `contextRequireImpulse`, `allowSideways`
2. **Entry gate** (`evaluateEntry`) — ATR band, swings, Fib retrace zone, R:R ≥ 1.5
3. **Strategy engine** — `waitForNextCandleClose`, `onePositionPerSymbol`, pending signal TTL
4. **Config table** — every `strategy.*`, `timeframes.*`, `sentiment.rules.strongNewsThreshold` affecting MTF
5. **Gaps** — items mentioned in ROADMAP/CONCERNS but not implemented (e.g. explicit “confirm rules”, cooldown if absent)
6. **Baseline reference** — mock backtest: 25 trades, 32% winRate (Phase 1)

Link line numbers to `mtf-engine.ts` and `strategy-engine.ts`.
  </action>
  <verify>test -f .planning/phases/04-mtf-entry-alignment/MTF-ENTRY-RULES.md</verify>
  <done>Doc covers context + entry + strategy queue with config keys</done>
</task>

</tasks>

<success_criteria>
- Team can read one doc to know why entries are blocked
</success_criteria>

<output>
Create `04-01-SUMMARY.md` after execution.
</output>
