---
phase: 04-mtf-entry-alignment
plan: 04
type: execute
depends_on: ["04-03"]
files_modified:
  - config/experiments/mtf-tf-4h-1h.yaml
  - config/experiments/mtf-tf-1d-1h.yaml
  - config/experiments/mtf-matrix-timeframes.yaml
  - .planning/phases/04-mtf-entry-alignment/MTF-TIMEFRAME-COMPARISON.md
domain: node-trading-bot
---

<objective>
Compare context/entry timeframe pairs against default 1d/4h.

Purpose: Timing vs noise tradeoff may dominate Fib/Elliott fit.
Output: `MTF-TIMEFRAME-COMPARISON.md` + small matrix results.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-mtf-entry-alignment/mtf-matrix-results.json
@config/default.yaml
@src/market/kline-cache.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeframe variant configs</name>
  <files>config/experiments/mtf-tf-4h-1h.yaml, config/experiments/mtf-tf-1d-1h.yaml, config/experiments/mtf-matrix-timeframes.yaml</files>
  <action>
From `mtf-baseline.yaml`, create:

- `mtf-tf-4h-1h.yaml` — `timeframes: { context: 4h, entry: 1h }`
- `mtf-tf-1d-1h.yaml` — `timeframes: { context: 1d, entry: 1h }`

`mtf-matrix-timeframes.yaml` — 3 runs: `mtf-baseline` (1d/4h), `mtf-tf-4h-1h`, `mtf-tf-1d-1h`, `mockSentiment: true`, same window/dir.

Ensure kline cache can load 1h intervals (download if missing; `skipDownload` false for first run if cache empty).
  </action>
  <verify>npm run backtest-matrix -- --matrix config/experiments/mtf-matrix-timeframes.yaml --dry-run</verify>
  <done>3-run timeframe matrix defined</done>
</task>

<task type="auto">
  <name>Task 2: Run and document</name>
  <files>.planning/phases/04-mtf-entry-alignment/MTF-TIMEFRAME-COMPARISON.md</files>
  <action>
Run timeframe matrix. Write `MTF-TIMEFRAME-COMPARISON.md`:

- Metrics table per TF pair
- Which pair best balances winRate vs totalTrades
- Caveat: changing TF invalidates direct comparison to Phase 1 unless baseline re-run on same cache window

Recommend whether to change default `timeframes` in Phase 6 (no merge yet).
  </action>
  <verify>test -f .planning/phases/04-mtf-entry-alignment/MTF-TIMEFRAME-COMPARISON.md</verify>
  <done>Comparison doc with recommendation section</done>
</task>

</tasks>

<success_criteria>
- Timeframe variants evaluated with same mock-sentiment protocol
</success_criteria>

<output>
Create `04-04-SUMMARY.md` after execution.
</output>
