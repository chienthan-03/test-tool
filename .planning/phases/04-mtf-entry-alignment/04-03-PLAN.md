---
phase: 04-mtf-entry-alignment
plan: 03
type: execute
depends_on: ["04-02"]
files_modified:
  - config/experiments/mtf-baseline.yaml
  - config/experiments/mtf-tighter-fib.yaml
  - config/experiments/mtf-require-impulse.yaml
  - config/experiments/mtf-higher-min-atr.yaml
  - config/experiments/mtf-no-wait-candle.yaml
  - config/experiments/mtf-stricter-swing.yaml
  - config/experiments/mtf-matrix.yaml
  - .planning/phases/04-mtf-entry-alignment/mtf-matrix-results.json
domain: node-trading-bot
---

<objective>
Create MTF experiment configs and run the main backtest matrix with mock sentiment.

Purpose: Quantify which strategy knobs move win rate vs trade count.
Output: `data/reports/experiments/mtf-phase4/` + `mtf-matrix-results.json`.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-mtf-entry-alignment/MTF-EXPERIMENT-GRID.md
@config/experiments/baseline-mock.yaml
@scripts/run-backtest-matrix.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MTF preset YAML files</name>
  <files>config/experiments/mtf-*.yaml</files>
  <action>
Copy `config/experiments/baseline-mock.yaml` as `mtf-baseline.yaml` (or alias same settings).

Create one-file-one-change presets per grid:

- `mtf-tighter-fib.yaml` — `strategy.fibonacci.zoneTolerancePercent: 0.02` (was 0.05)
- `mtf-require-impulse.yaml` — `strategy.elliott.contextRequireImpulse: true`
- `mtf-higher-min-atr.yaml` — `strategy.minAtrPercent: 0.18` (was 0.12)
- `mtf-no-wait-candle.yaml` — `strategy.entry.waitForNextCandleClose: false`
- `mtf-stricter-swing.yaml` — `strategy.swing.minSwingCount: 7` (was 5)

Use `sentiment-recommended` LLM-off block only if copying full yaml — keep sentiment identical across MTF runs.

Create `mtf-matrix.yaml`:
```yaml
from: "2024-10-01"
to: "2024-12-31"
mockSentiment: true
experimentsDir: ./data/reports/experiments/mtf-phase4
runs: [ six entries ]
```
  </action>
  <verify>npm run lint && npm run backtest-matrix -- --matrix config/experiments/mtf-matrix.yaml --dry-run</verify>
  <done>6 presets + dry-run OK</done>
</task>

<task type="auto">
  <name>Task 2: Run matrix and snapshot results</name>
  <files>data/reports/experiments/mtf-phase4/experiments-index.json, .planning/phases/04-mtf-entry-alignment/mtf-matrix-results.json</files>
  <action>
```bash
npm run backtest-matrix -- --matrix config/experiments/mtf-matrix.yaml
```

Copy `experiments-index.json` to `.planning/phases/04-mtf-entry-alignment/mtf-matrix-results.json` with note comparing to Phase 1 baseline (25 / 32%).

If a run errors, record in SUMMARY — do not omit from index.
  </action>
  <verify>test -f .planning/phases/04-mtf-entry-alignment/mtf-matrix-results.json</verify>
  <done>Index has 6 runs with metrics</done>
</task>

</tasks>

<verification>
- [ ] All runs use `mockSentiment: true`
- [ ] COMPARISON.md written under mtf-phase4
</verification>

<success_criteria>
- Measurable spread in winRate or totalTrades across presets OR documented why flat
</success_criteria>

<output>
Create `04-03-SUMMARY.md` after execution.
</output>
